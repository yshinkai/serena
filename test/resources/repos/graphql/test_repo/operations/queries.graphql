query GetUser($id: ID!) {
  user(id: $id) {
    id
    name
    email
    role
  }
}

query GetPosts {
  posts {
    id
    title
    published
    author {
      name
    }
  }
}

query SearchUsers($name: String!) {
  searchUsers(name: $name) {
    id
    name
  }
}
